import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mg.cloud.enchereapp',
  appName: 'EnchereApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
